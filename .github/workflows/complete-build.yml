# .github/workflows/complete-build.yml
name: Complete Build Pipeline

on:
  push:
    branches: [main, develop]
    paths-ignore:
      - '**/*.md'
      - '**/*.txt'
      - '.gitignore'
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - extension-only
          - mobile-only
      mobile_platform:
        description: 'Mobile platform to build'
        required: false
        default: 'android'
        type: choice
        options:
          - all
          - android
          - ios
      mobile_profile:
        description: 'EAS build profile'
        required: false
        default: 'development'
        type: choice
        options:
          - development
          - preview
          - production
      skip_tests:
        description: 'Skip tests'
        required: false
        default: false
        type: boolean
      skip_backend:
        description: 'Skip backend build'
        required: false
        default: false
        type: boolean
      skip_mobile:
        description: 'Skip mobile builds'
        required: false
        default: false
        type: boolean

env:
  NODE_VERSION: '20'
  ROOT_DIR: '${{ github.workspace }}'
  SKIP_MOBILE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.skip_mobile == 'true' }}

jobs:
  preflight:
    runs-on: ubuntu-latest
    name: Preflight Checks
    outputs:
      yarn_available: ${{ steps.check-yarn.outputs.yarn_available }}
      codegen_exists: ${{ steps.check-codegen.outputs.codegen_exists }}
      lint_exists: ${{ steps.check-lint.outputs.lint_exists }}
      package_vsix_exists: ${{ steps.check-package-vsix.outputs.package_vsix_exists }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js with Corepack
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'
          corepack: true

      - name: Enable Corepack
        run: corepack enable

      - name: Check Yarn availability
        id: check-yarn
        run: |
          if command -v yarn >/dev/null 2>&1; then
            echo "yarn_available=true" >> $GITHUB_OUTPUT
          else
            echo "Installing yarn via corepack..."
            corepack prepare yarn@1.22.22 --activate
            echo "yarn_available=true" >> $GITHUB_OUTPUT
          fi

      - name: Check for codegen script
        id: check-codegen
        run: |
          if jq -e '.scripts.codegen' package.json >/dev/null 2>&1; then
            echo "codegen_exists=true" >> $GITHUB_OUTPUT
          else
            echo "codegen_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Check for lint script
        id: check-lint
        run: |
          if jq -e '.scripts.lint' package.json >/dev/null 2>&1; then
            echo "lint_exists=true" >> $GITHUB_OUTPUT
          else
            echo "lint_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Check for package-vsix script
        id: check-package-vsix
        run: |
          if [ -f "scripts/package-vsix.sh" ]; then
            echo "package_vsix_exists=true" >> $GITHUB_OUTPUT
          else
            echo "package_vsix_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Check .env file
        run: |
          if [ ! -f ".env" ]; then
            if [ -f ".env.example" ]; then
              echo "Creating .env from .env.example template..."
              cp .env.example .env
              echo "Note: Update LOCAL_IP in .env for mobile connectivity"
            else
              echo "Warning: No .env or .env.example file found"
            fi
          fi

  validate-and-test:
    runs-on: ubuntu-latest
    name: Validate & Test
    needs: preflight
    timeout-minutes: 15
    if: ${{ !inputs.skip_tests || github.event_name == 'workflow_dispatch' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js with Corepack
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'
          corepack: true

      - name: Enable Corepack
        run: corepack enable

      - name: Get yarn cache directory
        id: yarn-cache-dir
        run: echo "dir=$(yarn cache dir)" >> $GITHUB_OUTPUT

      - name: Cache yarn dependencies
        uses: actions/cache@v3
        with:
          path: ${{ steps.yarn-cache-dir.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install dependencies
        run: yarn install --frozen-lockfile --ignore-engines --ignore-scripts

      - name: Generate GraphQL artifacts
        if: needs.preflight.outputs.codegen_exists == 'true'
        run: yarn codegen

      - name: TypeScript compilation check
        run: yarn tsc --noEmit

      - name: Run lint
        if: needs.preflight.outputs.lint_exists == 'true'
        run: yarn lint

      - name: Run backend tests
        run: |
          cd apps/backend
          npm test -- --coverage --passWithNoTests
          cd ../..

      - name: Upload test coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./apps/backend/coverage/lcov.info
          flags: backend
          name: backend-coverage

  build-backend:
    runs-on: ubuntu-latest
    name: Build Backend
    needs: [preflight, validate-and-test]
    if: ${{ !inputs.skip_backend && (github.event.inputs.build_type == 'all' || github.event.inputs.build_type == 'extension-only' || github.event_name != 'workflow_dispatch') }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js with Corepack
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'
          corepack: true

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --frozen-lockfile --ignore-engines --ignore-scripts

      - name: Generate GraphQL artifacts
        if: needs.preflight.outputs.codegen_exists == 'true'
        run: yarn codegen

      - name: Build backend
        run: yarn build:backend

      - name: Package VSIX (using package-vsix.sh if available)
        id: package-vsix
        run: |
          if [ -f "scripts/package-vsix.sh" ]; then
            echo "Using package-vsix.sh script..."
            chmod +x scripts/package-vsix.sh
            bash scripts/package-vsix.sh
            if [ -f "mobile-vscode-server.vsix" ]; then
              echo "VSIX created in root directory"
              echo "vsix_path=mobile-vscode-server.vsix" >> $GITHUB_OUTPUT
            elif [ -f "apps/backend/mobile-vscode-server.vsix" ]; then
              echo "VSIX created in apps/backend"
              echo "vsix_path=apps/backend/mobile-vscode-server.vsix" >> $GITHUB_OUTPUT
            else
              echo "VSIX not found, falling back to direct packaging"
              cd apps/backend
              npx @vscode/vsce package --no-dependencies --no-verify --out mobile-vscode-server.vsix
              echo "vsix_path=apps/backend/mobile-vscode-server.vsix" >> $GITHUB_OUTPUT
            fi
          else
            echo "package-vsix.sh not found, using direct packaging..."
            cd apps/backend
            npx @vscode/vsce package --no-dependencies --no-verify --out mobile-vscode-server.vsix
            echo "vsix_path=apps/backend/mobile-vscode-server.vsix" >> $GITHUB_OUTPUT
          fi

      - name: Create GitHub Release (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: MobileVSCode ${{ github.ref_name }}
          body: |
            ## MobileVSCode Extension ${{ github.ref_name }}

            ### What's Included
            - GraphQL API server for mobile IDE
            - Real-time collaboration with Yjs
            - Git integration
            - File system watching
            - VS Code extension management

            ### Installation
            1. Download the `.vsix` file
            2. In VS Code: `Extensions` → `...` → `Install from VSIX`
            3. Restart VS Code

            ### Requirements
            - VS Code 1.80.0 or higher
            - Node.js 18+

            ### Notes
            - First run will generate pairing token
            - Configure HTTPS certificates for production use
          files: |
            ${{ steps.package-vsix.outputs.vsix_path }}
          draft: false
          prerelease: ${{ contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') || contains(github.ref_name, 'rc') }}

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: mobile-vscode-extension-${{ github.sha }}
          path: ${{ steps.package-vsix.outputs.vsix_path }}
          retention-days: 30
          if-no-files-found: error

  build-mobile:
    runs-on: ${{ matrix.runner }}
    name: Build Mobile (${{ matrix.platform }})
    needs: [preflight, validate-and-test]
    if: ${{ (github.event_name != 'workflow_dispatch' || github.event.inputs.skip_mobile != 'true') && (github.event_name != 'workflow_dispatch' || github.event.inputs.build_type == 'all' || github.event.inputs.build_type == 'mobile-only') }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: android
            runner: ubuntu-latest
            if_condition: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.mobile_platform == 'all' || github.event.inputs.mobile_platform == 'android' }}
          - platform: ios
            runner: macos-latest
            if_condition: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.mobile_platform == 'all' || github.event.inputs.mobile_platform == 'ios' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        if: ${{ matrix.if_condition }}

      - name: Setup Node.js with Corepack
        uses: actions/setup-node@v4
        if: ${{ matrix.if_condition }}
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'
          corepack: true

      - name: Enable Corepack
        if: ${{ matrix.if_condition }}
        run: corepack enable

      - name: Install dependencies
        if: ${{ matrix.if_condition }}
        run: yarn install --frozen-lockfile --ignore-engines --ignore-scripts

      - name: Install EAS CLI
        if: ${{ matrix.if_condition }}
        run: npm install --global eas-cli

      - name: Generate GraphQL artifacts
        if: ${{ matrix.if_condition && needs.preflight.outputs.codegen_exists == 'true' }}
        run: yarn codegen

      - name: Build shared packages
        if: ${{ matrix.if_condition }}
        run: |
          yarn workspaces foreach --all -p run build 2>/dev/null || true

      - name: Check EAS authentication + required secrets
        if: ${{ matrix.if_condition }}
        id: check-eas-auth
        run: |
          set -euo pipefail

          if [ -n "${{ secrets.EXPO_TOKEN }}" ]; then
            echo "Using EXPO_TOKEN from secrets"
            echo "eas_authenticated=true" >> $GITHUB_OUTPUT
          elif eas whoami >/dev/null 2>&1; then
            echo "EAS already authenticated"
            echo "eas_authenticated=true" >> $GITHUB_OUTPUT
          else
            echo "EAS not authenticated and no EXPO_TOKEN provided"
            echo "eas_authenticated=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          if [ -z "${{ secrets.EAS_PROJECT_ID }}" ]; then
            echo "::error::Missing secrets.EAS_PROJECT_ID (required for non-interactive EAS builds)."
            echo "eas_authenticated=false" >> $GITHUB_OUTPUT
            exit 0
          fi

      - name: Ensure apps/mobile has eas.json (no requireCommit)
        if: ${{ matrix.if_condition && steps.check-eas-auth.outputs.eas_authenticated == 'true' }}
        run: |
          set -euo pipefail
          cd apps/mobile

          if [ ! -f "eas.json" ]; then
            echo "Creating apps/mobile/eas.json..."
            python - <<'PY'
            import json
            from pathlib import Path
            cfg = {
              "cli": {
                "version": ">= 16.0.0",
                "appVersionSource": "remote",
                "requireCommit": False
              },
              "build": {
                "development": {
                  "distribution": "internal",
                  "android": {"buildType": "apk"},
                  "ios": {"simulator": True}
                },
                "preview": {
                  "distribution": "internal",
                  "android": {"buildType": "apk"},
                  "ios": {"simulator": False}
                },
                "production": {
                  "distribution": "store",
                  "android": {"buildType": "app-bundle"},
                  "ios": {"simulator": False}
                }
              }
            }
            Path("eas.json").write_text(json.dumps(cfg, indent=2) + "\n")
            print("apps/mobile/eas.json written")
            PY
          else
            echo "apps/mobile/eas.json already exists"
          fi

          cd ../..

      - name: Link EAS project (CI-safe) and keep working tree clean
        if: ${{ matrix.if_condition && steps.check-eas-auth.outputs.eas_authenticated == 'true' }}
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EAS_PROJECT_ID: ${{ secrets.EAS_PROJECT_ID }}
        run: |
          set -euo pipefail

          # Link the EAS project in the app directory.
          # Some EAS commands may touch .expo/settings.json and/or app.json in that directory.
          cd apps/mobile

          eas project:init --non-interactive --id "${EAS_PROJECT_ID}" --force

          # IMPORTANT:
          # EAS build in non-interactive mode requires a clean working tree.
          # If EAS init touched tracked files, restore them so the tree is clean.
          # (This avoids the "working tree is dirty" abort you hit.)
          if git status --porcelain | grep -q .; then
            echo "Working tree changed during EAS init. Restoring tracked changes..."
            git restore --worktree --staged . || true
            git checkout -- . || true
          fi

          echo "Working tree status after restore:"
          git status --porcelain || true

          cd ../..

      - name: Build with EAS (remote) + download artifact (clean tree enforced)
        if: ${{ matrix.if_condition && steps.check-eas-auth.outputs.eas_authenticated == 'true' }}
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EAS_PROJECT_ID: ${{ secrets.EAS_PROJECT_ID }}
        run: |
          set -euo pipefail
          mkdir -p artifacts
          cd apps/mobile

          # Double-check clean working tree before build to prevent EAS abort.
          if git status --porcelain | grep -q .; then
            echo "::error::apps/mobile working tree is dirty before build. Aborting to avoid EAS clean-tree failure."
            git status --porcelain
            exit 1
          fi

          PROFILE="${{ github.event.inputs.mobile_profile || 'development' }}"
          PLATFORM="${{ matrix.platform }}"

          echo "Building for ${PLATFORM} with profile: ${PROFILE}"
          echo "Running: eas build --non-interactive --profile ${PROFILE} --platform ${PLATFORM} --wait --json"

          BUILD_JSON="$(mktemp)"
          set +e
          eas build --non-interactive --profile "${PROFILE}" --platform "${PLATFORM}" --wait --json | tee "${BUILD_JSON}"
          BUILD_STATUS=${PIPESTATUS[0]}
          set -e

          if [ "${BUILD_STATUS}" -ne 0 ]; then
            echo "EAS build failed (status=${BUILD_STATUS}), but continuing workflow..."
            echo "No artifact will be downloaded."
            exit 0
          fi

          BUILD_ID="$(python - <<'PY'
          import json, sys
          p = sys.argv[1]
          data = json.load(open(p))
          if isinstance(data, list):
            data = data[0] if data else {}
          print(data.get("id",""))
          PY
          "${BUILD_JSON}")"

          if [ -z "${BUILD_ID}" ]; then
            echo "Could not read build id from EAS JSON output. Skipping download."
            cat "${BUILD_JSON}"
            exit 0
          fi

          echo "Build ID: ${BUILD_ID}"

          VIEW_JSON="$(mktemp)"
          eas build:view "${BUILD_ID}" --json > "${VIEW_JSON}"

          ARTIFACT_URL="$(python - <<'PY'
          import json, sys
          data = json.load(open(sys.argv[1]))

          candidates = []

          artifacts = data.get("artifacts") or {}
          if isinstance(artifacts, dict):
            for k in ("applicationArchiveUrl", "artifactUrl", "url", "buildUrl"):
              v = artifacts.get(k)
              if isinstance(v, str) and v.startswith("http"):
                candidates.append(v)

          v = data.get("buildArtifactsUrl")
          if isinstance(v, str) and v.startswith("http"):
            candidates.append(v)

          a = data.get("buildArtifacts")
          if isinstance(a, list):
            for item in a:
              if isinstance(item, dict):
                v = item.get("url") or item.get("artifactUrl") or item.get("applicationArchiveUrl")
                if isinstance(v, str) and v.startswith("http"):
                  candidates.append(v)

          print(candidates[0] if candidates else "")
          PY
          "${VIEW_JSON}")"

          if [ -z "${ARTIFACT_URL}" ]; then
            echo "Could not find an artifact URL in build:view output. Skipping download."
            cat "${VIEW_JSON}"
            exit 0
          fi

          echo "Artifact URL: ${ARTIFACT_URL}"

          EXT="bin"
          case "${ARTIFACT_URL}" in
            *.apk*) EXT="apk" ;;
            *.aab*) EXT="aab" ;;
            *.ipa*) EXT="ipa" ;;
            *.tar.gz*) EXT="tar.gz" ;;
            *.tgz*) EXT="tgz" ;;
          esac

          OUT="../artifacts/mobile-vscode-${PLATFORM}-${PROFILE}.${EXT}"
          echo "Downloading to: ${OUT}"
          curl -fL --retry 3 --retry-delay 2 -o "${OUT}" "${ARTIFACT_URL}"

          echo "Artifacts directory:"
          ls -lah "../artifacts"

          cd ../..

      - name: Upload Android artifacts
        if: ${{ matrix.if_condition && steps.check-eas-auth.outputs.eas_authenticated == 'true' && matrix.platform == 'android' }}
        uses: actions/upload-artifact@v4
        with:
          name: mobile-vscode-android-${{ github.event.inputs.mobile_profile || 'development' }}-${{ github.sha }}
          path: artifacts/mobile-vscode-android-*
          retention-days: 30
          if-no-files-found: warn

      - name: Upload iOS artifacts
        if: ${{ matrix.if_condition && steps.check-eas-auth.outputs.eas_authenticated == 'true' && matrix.platform == 'ios' }}
        uses: actions/upload-artifact@v4
        with:
          name: mobile-vscode-ios-${{ github.event.inputs.mobile_profile || 'development' }}-${{ github.sha }}
          path: artifacts/mobile-vscode-ios-*
          retention-days: 30
          if-no-files-found: warn

  deploy-preview:
    runs-on: ubuntu-latest
    name: Deploy Preview
    if: github.event_name == 'pull_request'
    needs: [validate-and-test, build-backend, build-mobile]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate QR Code for Preview
        uses: crazy-max/ghaction-qrcode@v3
        with:
          text: |
            📱 MobileVSCode Preview Build

            Branch: ${{ github.head_ref }}
            Commit: ${{ github.sha }}

            Scan to open in Expo Go app!

            Artifacts: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            Server Endpoint: wss://mobile-vscode-${{ github.head_ref }}.example.com
          size: 300
          output: qr-preview.png

      - name: Comment Preview Links on PR
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const qrBase64 = fs.readFileSync('qr-preview.png', { encoding: 'base64' });

            const comment = {
              body: `## 🚀 MobileVSCode Preview Build\n\n` +
                    `**Branch:** ${context.payload.pull_request.head.ref}\n` +
                    `**Commit:** ${context.sha.substring(0, 7)}\n` +
                    `**Build:** #${context.runNumber}\n\n` +
                    `### 📦 Built Artifacts:\n` +
                    `- [VS Code Extension (.vsix)](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}/artifacts)\n` +
                    `- [Android APK](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}/artifacts)\n\n` +
                    `### 📱 Expo Preview\n` +
                    `![QR Code](data:image/png;base64,${qrBase64})\n\n` +
                    `_Scan with Expo Go app (Android/iOS)_\n\n` +
                    `### 🔧 Deployment Instructions:\n` +
                    `1. Download the VSIX file and install in VS Code\n` +
                    `2. Start the MobileVSCode server from VS Code\n` +
                    `3. Download the Android APK on your device\n` +
                    `4. Pair your mobile device with the pairing token\n\n` +
                    `---\n` +
                    `*Generated by GitHub Actions*`
            };

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment.body
            });

  summary:
    runs-on: ubuntu-latest
    name: Build Summary
    needs: [preflight, validate-and-test, build-backend, build-mobile]
    if: always()

    steps:
      - name: Generate Build Summary
        run: |
          echo "## 📊 MobileVSCode Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Workflow:** \`${{ github.workflow }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Run:** [#${{ github.run_number }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** [${{ github.sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### 📈 Build Status" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Status | Artifacts |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|--------|-----------|" >> $GITHUB_STEP_SUMMARY

          if [[ "${{ needs.preflight.result }}" == "success" ]]; then
            echo "| ✅ Preflight Checks | ✅ Success | - |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ Preflight Checks | ❌ Failed | - |" >> $GITHUB_STEP_SUMMARY
          fi

          if [[ "${{ needs.validate-and-test.result }}" == "success" ]]; then
            echo "| ✅ Validate & Test | ✅ Success | [Coverage Report](https://app.codecov.io/gh/${{ github.repository }}) |" >> $GITHUB_STEP_SUMMARY
          elif [[ "${{ needs.validate-and-test.result }}" == "skipped" ]]; then
            echo "| ⚠️ Validate & Test | ⚠️ Skipped | - |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ Validate & Test | ❌ Failed | - |" >> $GITHUB_STEP_SUMMARY
          fi

          if [[ "${{ needs.build-backend.result }}" == "success" ]]; then
            echo "| ✅ VS Code Extension | ✅ Success | [Download VSIX](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts) |" >> $GITHUB_STEP_SUMMARY
          elif [[ "${{ needs.build-backend.result }}" == "skipped" ]]; then
            echo "| ⚠️ VS Code Extension | ⚠️ Skipped | - |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ VS Code Extension | ❌ Failed | - |" >> $GITHUB_STEP_SUMMARY
          fi

          MOBILE_BUILT=false
          if [[ "${{ needs.build-mobile.result }}" == "success" ]]; then
            MOBILE_BUILT=true
          fi

          if [[ "$MOBILE_BUILT" == "true" ]]; then
            echo "| ✅ Mobile App | ✅ Success | [Download APK/IPA](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts) |" >> $GITHUB_STEP_SUMMARY
          elif [[ "${{ github.event.inputs.skip_mobile }}" == "true" ]]; then
            echo "| ⚠️ Mobile App | ⚠️ Skipped | - |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ Mobile App | ❌ Failed | - |" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Next Steps:" >> $GITHUB_STEP_SUMMARY
          echo '1. **Install Extension:** Download the `.vsix` file and install in VS Code' >> $GITHUB_STEP_SUMMARY
          echo '2. **Start Server:** Run `Mobile VSCode: Start Server` from VS Code command palette' >> $GITHUB_STEP_SUMMARY
          echo "3. **Install Mobile App:** Download the APK/IPA file to your device" >> $GITHUB_STEP_SUMMARY
          echo "4. **Pair Device:** Open the mobile app and enter the pairing token from VS Code" >> $GITHUB_STEP_SUMMARY
          echo "5. **Development:** Use VS Code to edit files that sync to your mobile device in real-time" >> $GITHUB_STEP_SUMMARY

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔧 Configuration Notes:" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Profile:** \`${{ github.event.inputs.mobile_profile || 'development' }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Platforms Built:** \`${{ github.event.inputs.mobile_platform || 'android' }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Skip Tests:** \`${{ github.event.inputs.skip_tests || false }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Skip Backend:** \`${{ github.event.inputs.skip_backend || false }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Skip Mobile:** \`${{ github.event.inputs.skip_mobile || false }}\`" >> $GITHUB_STEP_SUMMARY
