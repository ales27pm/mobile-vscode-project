import 'react-native-get-random-values';
import 'react-native-url-polyfill';

// Keep additional polyfills centralized so Metro always resolves the same module
// (avoid ambiguous file/directory names).
import './polyfills/crypto';
